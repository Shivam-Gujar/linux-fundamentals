# Basic Linux Commands

## ls
Lists directory contents.

Example:
ls -lh /var/log

Why it matters:
Used to inspect log files and directories on servers.

## cd
Changes directory.

Example:
cd /etc/nginx

## pwd
Prints current working directory.

Used in scripts to confirm execution path.

